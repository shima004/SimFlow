apiVersion: apps/v1
kind: Deployment
metadata:
  name: simflow
  namespace: simflow
  labels:
    app: simflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: simflow
  template:
    metadata:
      labels:
        app: simflow
    spec:
      serviceAccountName: simflow
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: simflow-data
      containers:
        - name: simflow
          image: simflow:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            # --- ConfigMap (non-sensitive) ---
            - name: ORIGIN
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: ORIGIN
            - name: ADMIN_SUBJECT
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: ADMIN_SUBJECT
                  optional: true
            # Argo Workflows
            - name: ARGO_BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: ARGO_BASE_URL
            - name: ARGO_NAMESPACE
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: ARGO_NAMESPACE
            # S3
            - name: S3_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: S3_ENDPOINT
                  optional: true
            - name: S3_REGION
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: S3_REGION
                  optional: true
            - name: S3_ALLOWED_BUCKETS
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: S3_ALLOWED_BUCKETS
            - name: S3_AGENTS_BUCKET
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: S3_AGENTS_BUCKET
                  optional: true
            - name: S3_MAPS_BUCKET
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: S3_MAPS_BUCKET
                  optional: true
            # Workflow templates
            - name: WORKFLOW_TEMPLATES
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: WORKFLOW_TEMPLATES
                  optional: true
            # SimScope viewer
            - name: SIMSCOPE_BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: SIMSCOPE_BASE_URL
                  optional: true
            - name: SIMSCOPE_LOG_BUCKET
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: SIMSCOPE_LOG_BUCKET
                  optional: true
            - name: SIMSCOPE_PORT
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: SIMSCOPE_PORT
                  optional: true
            - name: SIMSCOPE_NAMESPACE
              valueFrom:
                configMapKeyRef:
                  name: simflow-config
                  key: SIMSCOPE_NAMESPACE
                  optional: true
            # --- Secret (sensitive) ---
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: simflow-secret
                  key: JWT_SECRET
            - name: ARGO_TOKEN
              valueFrom:
                secretKeyRef:
                  name: simflow-secret
                  key: ARGO_TOKEN
                  optional: true
            # S3 static credentials (omit when using IRSA)
            - name: S3_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: simflow-secret
                  key: S3_ACCESS_KEY_ID
                  optional: true
            - name: S3_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: simflow-secret
                  key: S3_SECRET_ACCESS_KEY
                  optional: true
            # SQLite database path
            - name: DB_PATH
              value: /data/simflow.db
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /login
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /login
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 30
