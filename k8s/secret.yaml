# Template for Secret — fill in base64-encoded values before applying.
# Encode a value: echo -n 'your-value' | base64
#
# Apply with: kubectl apply -f k8s/secret.yaml
# DO NOT commit this file with real values.
apiVersion: v1
kind: Secret
metadata:
  name: simflow-secret
type: Opaque
data:
  # Argo Workflows
  ARGO_BASE_URL: ""        # e.g. echo -n 'http://argo-server:2746' | base64
  ARGO_NAMESPACE: ""       # e.g. echo -n 'argo' | base64
  ARGO_TOKEN: ""           # e.g. echo -n 'Bearer v2:...' | base64
  # S3-compatible storage
  S3_ENDPOINT: ""          # e.g. echo -n 'http://minio:9000' | base64
  S3_REGION: ""            # e.g. echo -n 'us-east-1' | base64
  S3_ACCESS_KEY_ID: ""     # e.g. echo -n 'minioadmin' | base64
  S3_SECRET_ACCESS_KEY: "" # e.g. echo -n 'minioadmin' | base64
  S3_ALLOWED_BUCKETS: ""   # e.g. echo -n 'agents,maps' | base64
  # Authentication
  JWT_SECRET: ""        # HS256: echo -n 'your-secret' | base64
  # JWT_PUBLIC_KEY: "" # RS256: base64-encoded PEM public key (set instead of JWT_SECRET)
  # JWT_ALGORITHM: ""  # e.g. RS256 (default: RS256 when JWT_PUBLIC_KEY is set)
  ADMIN_SUBJECT: ""     # JWT sub of the first admin user (auto-created on first login)
  # SimScope viewer (optional)
  SIMSCOPE_BASE_URL: ""       # e.g. echo -n 'http://simscope:3000' | base64
  SIMSCOPE_LOG_BUCKET: ""     # e.g. echo -n 'rrs-logs' | base64
  SIMSCOPE_PORT: ""           # e.g. echo -n '27931' | base64
  SIMSCOPE_NAMESPACE: ""      # e.g. echo -n 'argo' | base64  (omit to use workflow's namespace)
