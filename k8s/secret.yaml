# Template for Secret — fill in base64-encoded values before applying.
# Encode a value: echo -n 'your-value' | base64
#
# Apply with: kubectl apply -f k8s/secret.yaml
# DO NOT commit this file with real values.
apiVersion: v1
kind: Secret
metadata:
  name: simflow-secret
type: Opaque
data:
  # Argo Workflows
  ARGO_BASE_URL: ""        # e.g. echo -n 'http://argo-server:2746' | base64
  ARGO_NAMESPACE: ""       # e.g. echo -n 'argo' | base64
  ARGO_TOKEN: ""           # e.g. echo -n 'Bearer v2:...' | base64
  # S3-compatible storage
  S3_ENDPOINT: ""          # e.g. echo -n 'http://minio:9000' | base64
  S3_REGION: ""            # e.g. echo -n 'us-east-1' | base64
  S3_ACCESS_KEY_ID: ""     # e.g. echo -n 'minioadmin' | base64
  S3_SECRET_ACCESS_KEY: "" # e.g. echo -n 'minioadmin' | base64
  S3_ALLOWED_BUCKETS: ""   # e.g. echo -n 'agents,maps' | base64
