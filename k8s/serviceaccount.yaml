apiVersion: v1
kind: ServiceAccount
metadata:
  name: simflow
  namespace: simflow
  # For IRSA on EKS, add the IAM role annotation:
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::<account-id>:role/<role-name>
